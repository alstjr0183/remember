import { css } from "styled-components";

// colors
const colors = {
  // 블루계열
  blueLight: "#87b1f3",
  blue: "#2c5ae9",
  blueDark: "#2c5ae9",
  // 회색계열
};

// layout z-index

const theme = {
  colors,
};

export default theme;
